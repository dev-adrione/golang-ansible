# Golang Ansible Installer
This project Install Golang in an automated way using Ansible inside an Ubuntu-based Docker container
#How to use
....bash
docker build -t golang-ansible .
docker run -it golang-ansible .
